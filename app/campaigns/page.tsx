import { PageShell } from "@/components/page-shell";

export default function CampaignsPage() {
  return (
    <PageShell
      title="Campaigns"
      description="Browse open Content Rewards campaigns. Placeholder — discovery not built yet."
    />
  );
}
